{
  "name": "unified-panel",
  "version": "1.0.0",
  "description": "Dashboard global TikTok + YouTube — agrège les 2 panels en une seule UI.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "nodemon --exec 'node --env-file-if-exists=.env' server.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
